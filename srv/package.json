{
  "name": "livestore-srv",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@sap/cds": "^6.0.0",
    "express": "^4.17.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}